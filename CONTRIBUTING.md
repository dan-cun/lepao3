# 贡献指南（CONTRIBUTING）

## 0. 提交即接受协议
向本仓库提交 issue、PR、代码、抓包样本或文档，即表示你有权作出该贡献，并同意它按
**乐跑研究协议 1.0（LRL-1.0）**授权给所有使用者（详见 LICENSE 第十条）。不接受者请勿提交。

## 1. 欢迎的贡献
- 协议/字段语义的研究记录与可复现验证脚本（请脱敏）。
- 轨迹生成、反指纹判定、稳定性与错误恢复方面的改进。
- 文档、部署脚本、跨平台适配（macOS/Linux）。
- Bug 修复与测试。

## 2. 不接受的贡献
- **任何形式的商业化改造**（收费、会员、代打卡、加广告、闭源分发）—— 提交也不会合并，
  且这类使用本身不在授权范围内。
- 移除或改写版权声明块、LICENSE、NOTICE、DISCLAIMER（LRL-1.0 第三条：授权自动终止）。
- 把衍生版换成 MIT / GPL / Apache / 商业许可再发布（LRL-1.0 第四条：必须相同方式共享）。
- 冒用原作者名义、使用"乐跑打卡助手"或近似名称分发、保留作者 QQ 作为你的支持渠道。
- 含**真实身份信息**的提交：他人 token/session/cookie、学号、姓名、手机号、身份证、
  精确位置轨迹、服务端记录号、抓包原始文件。示例一律用 `XXXXXXXXX` / `0000000` 占位。
- 未获账号持有人明确同意而录入的成员账号信息（登记成员 = 白名单，须本人知情）。

## 3. PR 前的本地检查
```bash
py -3 tools/apply_headers.py --check      # 所有源文件声明块齐全且与规范一致
py -3 tools/verify_release.py             # PII 泄露扫描 + 声明完整性（发行门禁）
py -3 cli.py status --all                 # 冒烟：能读到注册表
py -3 ui/runner_ui.py --selfcheck         # GUI 自检（无显示环境可跳过）
```
新增 .py 文件后运行 `py -3 tools/apply_headers.py` 自动补声明块。

## 4. 分支与提交
- 目标分支 `main`；一次 PR 一件事，说明动机与验证方式。
- 涉及外部接口行为变更的 PR，请在描述中写明**验证证据**（脱敏日志片段/对照实验），
  不要只写"我这边能跑"。
- commit message 用中文或英文均可，首行 ≤ 60 字。

## 5. 数据与隐私纪律（硬约束）
- `data/`、`config.json`、`state.json`、`凭证.json`、`flows*`、`proxy_backup.json`、`logs/`
  永不入库（已在 .gitignore）。
- 提 PR 前执行 `py -3 tools/verify_release.py --staged` 扫描本次改动是否夹带敏感信息。
- 若不慎提交，请**立刻告知作者轮换/影响面**，仅删除文件不足以从 git 历史中消除痕迹。
