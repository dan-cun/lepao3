# 乐跑打卡助手 —— 独立部署（自动打卡程序，零个人信息）

> **授权先决条件**：本程序按 **乐跑研究协议 1.0（LRL-1.0）** 提供，**仅供学习研究、不可牟利**；
> 再发布（含你重打包的 EXE/压缩包）必须保留全部声明文件与源文件版权头，并署名引用原始仓库
> <https://github.com/dan-cun/lepao3>。技术讨论 QQ 1224145544。全文见 [LICENSE](LICENSE)。
>
> 移植版说明：程序体 / 协议库 / 桌面端 / 乐跑地图已完整就位，**不含任何个人身份信息**
> （无原机主的学号、uid、token、服务端记录号、抓包流量）；成员表为空，由本机使用者自行登记。
> 依赖：mitmweb + Clash（7897，缺失时自动走直连模式）+ 电脑版微信。

## ⚠ 用前必读：声明与许可不可删除（三分钟）

1. **不能删的东西**：`LICENSE`、`NOTICE`、`DISCLAIMER.md`、`SECURITY.md`、`CONTRIBUTING.md`，
   以及每个 `.py` 文件顶部的 `lepao-research-notice` 版权块。删掉即依 LRL-1.0 第三条
   **自动失去使用与分发授权**。程序启动时会校验随包 LICENSE 的 SHA-256，并在日志与界面打印警示；
   界面右下角常驻署名行，「关于·许可证」窗口展示条款摘要与构建戳（build id）。
2. **改完要再发布（爆改）**：在 ①你的 README 开头 ②程序运行时可见输出 ③「关于」界面 **三处**
   注明原作者 dan-cun 与原始仓库地址；写清你改了什么；把联系方式换成你自己的（不得保留作者 QQ
   作为你的支持渠道）；不得沿用「乐跑打卡助手」等易混淆名称；并继续以 **LRL-1.0** 授权，
   不得改换为 MIT / GPL / Apache / 商业许可。
   批量换地址可用 `py -3 tools/apply_headers.py --url <你的地址>` —— 它只能更新地址，
   **不能移除**版权块本身。
3. **不能拿来赚钱**：任何形式的对价都不行 —— 售卖副本、收费代打卡、付费群分享、广告变现、
   捆绑课程、自营工具站均在禁止之列。确有需要（教学课件、论文复现、非营利社团内训）
   请事先经 QQ 取得单独书面授权。
4. **只打本人或已明确同意者的卡**：登记成员 = 写入 mitm 白名单，须本人知情；
   他人凭证、位置与轨迹数据一律不碰、不存、不传。

## 目录结构
```
乐跑打卡助手\
├── 乐跑打卡助手.exe         # 桌面版 v1.2.0（双击即用；冻结态自检通过）
├── cli.py                   # 命令行入口（可选）
├── lepao\                   # 协议库: crypto/transport/v3api/accounts/auth/capture/
│                            #   track/record/pipeline/state/wechat_ctl/license_guard
├── data\真实轨迹_035明文.json   # ★ 乐跑地图（轨迹模板 226 点，服务端判有效的形态）
├── 工具\mitm_lptiyu_token.py    # mitm 抓号插件（白名单 = 已登记成员）
├── config.json              # v2 骨架：成员表为空 + 环境/策略默认值
├── config.sample.json       # 占位模板（仅参考）
├── tools\                   # apply_headers.py（版权头/构建戳）· verify_release.py（发行门禁）
├── LICENSE / NOTICE / DISCLAIMER.md / SECURITY.md / CONTRIBUTING.md   # 声明，不可删
└── ui\                      # 桌面端源码（可自行改版重打包）
```

## 首次使用（三步上线）
1. **双击 `乐跑打卡助手.exe`**
   - 若提示缺 mitmweb：安装 mitmproxy（或把可用的 `mitmweb.exe` 路径写入本目录
     `runner.json`：`{"mitmweb":"…"}`）；Clash 未开会自动进入直连模式。
   - 也可先跑一次 `.\一键配置.ps1 -InstallPython -InstallWechat` 自动定位/安装依赖。
2. **成员管理 → 新增成员**：填 姓名 + 学号（本人或已同意代打的同学；学号即 mitm 白名单键，
   未登记的登录流量不会落盘）。
3. **▶ 启动程序**：
   - 勾选「启动前强制退出微信登录」→ 程序结束当前微信并清登录态 → 重新唤起微信；
   - **扫码登录微信** → 只打开一次小程序「数体智慧体育」到跑步首页；
   - 识别到登录流量后自动 归位 → 提交 → 复核（此阶段勿再操作小程序，会顶号）；
   - 结果 = 「自动确认有效」即成功；全部日志在 `logs\runner_YYYYMMDD.log`。

## 命令行方式（可选）
```bash
cd <本程序所在目录>
py -3 cli.py account add <名字> <学号>     # 登记成员
py -3 cli.py run --member <学号>           # 一键：先退微信登录→扫码→自动打卡
py -3 cli.py wechat-logout / wechat-status # 手动退出微信 / 查看微信状态
py -3 cli.py status --all                  # 成员 token 状态
py -3 cli.py license                       # 打印许可证要点 / 署名 / 声明指纹 / 构建戳
```

## 数据与隐私边界
- 本目录不含任何他人凭据；运行后产生的新数据（config.json 成员、凭证、logs、flows、
  proxy_backup）只属于**本机使用者**，勿外传、勿提交 git（.gitignore 已排除）。
- 仅打卡已登记且同意的成员；每成员每日 ≤1 次有效；提交后 token 即消耗，次日需重新登录抓号。
- 地图/轨迹模板仅含跑道坐标序列（无身份字段），用于让服务端认可轨迹形态。
- **不要把 `data/` 换成含真实个人信息的数据后再分发**（verify_release.py 会阻断）。

## 自检命令
- `.\乐跑打卡助手.exe --selfcheck` → 写 `selfcheck.json`（mitmweb/Clash/微信/成员数/许可证状态）
- `py -3 cli.py quota --member <学号>` → 跑区/打卡点/当日额度（只读，需已抓号）
- `py -3 cli.py build --member <学号>` → 干跑（零写入）全流程验证
- `py -3 tools/verify_release.py` → 发行门禁：声明完整性 + 个人信息泄露扫描

## 免责
本程序按「现状」提供，无任何担保，不保证打卡必然成功或被服务端判定有效；
账号限制/封禁、成绩作废、数据丢失等使用后果由使用者自行承担。
本研究与乐跑平台、任何学校、腾讯公司无关联、无授权、无背书。完整条款见 `LICENSE`。
