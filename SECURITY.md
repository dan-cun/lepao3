# 漏洞与问题披露（SECURITY）

本项目是本地运行的协议研究工具，不联网提供服务、不收集任何遥测数据。
安全相关事项请按下述方式处理。

## 1. 报告渠道
- **QQ 1224145544**（首选，仅作技术讨论）。请附：版本号（见程序"关于"/`release.manifest.json` 的 `build_id`）、
  复现步骤、脱敏后的日志片段。
- 请**不要**在 issue / 群里粘贴：token、session、cookie、完整抓包流、他人学号/姓名/位置轨迹。
  这些属于敏感个人信息，一旦公开即无法撤回。

## 2. 已知且属于"设计边界"的事项（不视为漏洞）
1. **协议常量硬编码**：AES key/iv 与签名 secret 来自小程序客户端，无法由本项目轮换。
   任何人反编译小程序同样可获得，因此不构成额外泄露面。
2. **必须安装 mitmproxy 根证书**：不做中间人观测就无法取号。风险边界：
   请在自己完全拥有的设备上使用，用毕执行"■ 停止并复原"（恢复系统代理）；
   不要在被管控/公共设备上使用；排查完毕后建议删除该根证书。
3. **凭证本地明文存储**（`config.json` / `state.json` / `flows` / `凭证.json`）：依赖操作系统账户隔离。
   多用户共用电脑、云同步目录（OneDrive/坚果云等）会把凭证带出本机 —— 请把数据目录设在**非同步**路径。
4. **会强制退出微信进程**：仅结束客户端进程，不读取、不修改微信本地数据（见 `lepao/wechat_ctl.py`）。
   这是"避免同账号重复登录顶掉刚抓到的 token"的最小必要操作，可在设置中关闭。
5. **token 为一次性消耗品**：小程序被再次打开就会顶号。程序内置顶号看门狗，检测到即中止任务并明确报告，
   不会用错号提交。

## 3. 需要报告的情形
- 程序向作者或任何第三方**回传**数据（本项目无联网回传代码，若发现异常出网请求请报告，
  可用 `tools/verify_release.py --network` 或任何防火墙核对）。
- 发行包内含真实身份数据（学号、姓名、token、位置轨迹、记录号）—— 属严重事故，请立即报告。
- 任何可被利用来在**未经他人同意**的前提下取号或提交的缺陷（白名单绕过等）。

## 4. 发布前自检（维护者）
```bash
py -3 tools/apply_headers.py --check     # 源文件声明块是否齐全一致
py -3 tools/verify_release.py            # 发行前门禁：PII 泄露 / 声明缺失 / EXE 构建戳一致性
py -3 tools/verify_release.py --exe "乐跑打卡助手.exe"
```

## 5. 权利人通知（DMCA / 下架）
乐跑平台或腾讯公司如认为本项目不妥，请通过上述 QQ 联系，说明权利归属与要求范围，
作者将在核实后处理（包括修改、下架相应内容）。
